A simple data-cleaning script using Pandas.
It loads students.csv, removes duplicates, checks missing values, and computes summary statistics (mean, median, std) per student and subject.
Run with:

    python3 cleaning.py